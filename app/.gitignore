# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist build output is regenerated by `npm run build`, but dist/data holds the
# LIVE data files (CSVs, fonts, logos) the server reads - those must be tracked.
dist/*
!dist/data
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
